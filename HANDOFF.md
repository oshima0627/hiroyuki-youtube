# HANDOFF

最終更新: 2026-09-02

---

## ▶ このセッションで最初に実行するコマンド

**アナリティクスを見る。** 2026-09-02 に `scripts/analytics.py` を新設した。これ1本で足りる。

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/analytics.py
```

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/analytics.py --retention pCbEawE27Ds
```

### まだ手が付いていない、期限のあるもの

1. **ショート6本（09/18〜09/23）が未投稿。** ビルド・検証は済み。`published.json` に
   `2026-09-18-am`〜`2026-09-23-am` が無いことを 09/02 に確認した。
   ```bash
   cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/publish_shorts.py
   ```
2. **09/07 までに不動産回 `TtJKOPH0PCU` を公開する。** 09/07 と 09/14 のショート
   （`bZV29-wLKE8` / `xRNih8qkIdE`）がこれを親に持ち、概要欄が非公開動画を指している。
3. **在庫が 09/23 で尽きる。** `fetch_topics.py --save` で仕込む
   （素材の全編ダウンロードは不要。`--subs-only` ＋ `probe_signals.py --no-audio`）。

---

## いま何をしているのか

ひろゆき切り抜きチャンネル「ひろゆき解説ch【切り抜き】」(`UCqK3KYqEeeJiAWr4nSryJYQ` /
`@hiroyuki_kaisetsu`) の運用。

**跳ねた。** 08/26（PT）を境に日次 0〜15回 → 1,456〜2,338回。前セッションの
「ショートを毎日出し続けられていなかったのが問題」という読みは当たっていた。
**問題は配信量ではなくなった。次のボトルネックは登録者への転換（0.06%）。**

## 今回やったこと（2026-09-02）

| 変更 | 内容 |
|---|---|
| `scripts/analytics.py` | **新規。** YouTube Analytics API v2 を叩く。日次・動画別・流入元・検索語・年齢性別・端末・国・視聴維持率 |
| `scripts/upload_youtube.py` | `get_service()` から `get_credentials()` を切り出した（Analytics API は別サービスなので creds が要る）。`_cred_dir()` を追加し、git worktree から実行しても本体の `token.json` を見に行くようにした |

`yt-analytics.readonly` スコープは前から取ってあったが、一度も使われていなかった。

## 検証済みの事実（2026-09-02 に `python scripts/analytics.py` の出力で確認）

期間 2026-08-13〜2026-09-02。**Analytics のデータは 08/30 までしか無い**（2〜3日遅れる）。
日付は PT なので、JST 07:00 公開は前日の行に乗る。

### 1. 配信は解けた。SHORTS フィードから来ている

```
日次 views:  08/14-08/25 → 0〜265（大半が一桁）
             08/26 1603 / 08/27 1730 / 08/28 1456 / 08/29 2338 / 08/30 1984
流入元:      SHORTS 8259 / SUBSCRIBER 561 / RELATED_VIDEO 446 / YT_SEARCH 174
```

### 2. ショート1本は「1日で燃え尽きる」。積み上がらない

動画×日（views）。1本が公開日に 1,200〜1,600 取り、翌々日にはほぼ 0。

| videoId | 公開日 | +0日 | +1日 | +2日 | +3日 |
|---|---|---|---|---|---|
| `pCbEawE27Ds` | 08/26 | 1599 | 565 | 1 | 5 |
| `HCn_fSMbkDM` | 08/27 | 1158 | 8 | 228 | 0 |
| `IihXyEgECno` | 08/28 | 1446 | 472 | 235 | — |
| `Xhf0wKl-aBY` | 08/29 | 1447 | 191 | — | — |
| `706Seph9B2Q` | 08/30 | 1199 | — | — | — |

**どの本も 1,200〜2,200 に収まっていて、出来の差が到達数に効いていない。**
まだ「一定量のテスト配信を配られている」段階で、そこから伸びる側に抜けてはいない。

### 3. 登録者に変わっていない ← いまのボトルネック

```
subscribedStatus:  UNSUBSCRIBED 9604 views / SUBSCRIBED 6 views
期間中の subscribersGained: 合計 6（8/15に1、8/28に1、8/29に2、8/30に2）
```

9,604回見られて登録 6人 = **0.06%**。収益化はショート枠（登録1,000＋90日で1,000万回）でも
長尺枠（登録1,000＋365日で4,000時間）でも**登録1,000が必須**。いまの総視聴時間は約76時間。

動画別の `subscribersGained` の内訳: ショート3人（`IihXyEgECno` / `Xhf0wKl-aBY` / `706Seph9B2Q`
が各1）、長尺1人（`f_pERzvz3uA`）、残り2人は動画に紐付いていない。
**1再生あたりでは長尺のほうが高い**（長尺 200回で1人 = 0.5%、ショート 約8,600回で3人 = 0.035%）。
ただし分子が1と3なので断定はできない。

### 4. 視聴維持率の崖は「全部同じ場所」＝ 10〜20%（39〜45秒なので 4〜8秒目）

`audienceWatchRatio`（1.00 = その地点を平均1回見た）。5本すべてが 10% までは 1.2〜1.4 を
保ち、10%→20% で落ちる。**落ち幅だけが本ごとに違う。**

| videoId | 見出し | 1% | 10% | 20% | 100% | 平均視聴率 |
|---|---|---|---|---|---|---|
| `pCbEawE27Ds` | フランス人は歩きスマホをしない | 1.47 | 1.33 | **0.97** | 0.48 | **74.9%** |
| `HCn_fSMbkDM` | 資格は取れるが実務で通用するか不安 | 1.45 | 1.38 | **0.87** | 0.44 | 71.7% |
| `706Seph9B2Q` | 残業は増えるが年収が上がる転職 | 1.41 | 1.36 | **0.95** | 0.40 | 67.6% |
| `IihXyEgECno` | 「まずい」という言葉を使わない | 1.40 | 1.27 | **0.89** | 0.40 | 65.1% |
| `Xhf0wKl-aBY` | 降格されて新卒並みの待遇にされた | 1.31 | 1.21 | **0.68** | 0.30 | **54.5%** |

- 1% が 1.0 を超えている＝ループして2周目に入っている人がいる。**入口の掴みは効いている。**
- `relativeRetentionPerformance` は全本 0.44〜0.64。**同尺の他動画に対してほぼ中央値**で、
  抜けてはいない。
- **尺は効いていない。** 最長の45秒（`pCbEawE27Ds`）が最良、最短の39秒が最悪。

### 5. 視聴者

```
男性 85.5%（25-34 27.3% / 35-44 22.8% / 45-54 17.8%）
国 JP 100%（9,519）
端末 MOBILE 8124 / DESKTOP 840 / TABLET 390 / TV 239
```

### 6. 長尺は今の作り方では回らない

最良で `jaQZTRkMN4E` 332回。平均視聴率 26〜33%、`relativeRetentionPerformance` 0.25〜0.5
（中央値未満）。ただし RELATED_VIDEO から446回来ていて、**視聴時間の大半は長尺が稼いでいる**。

### 7. `probe_signals` の `score` は維持率を予測していない

最良の `pCbEawE27Ds` が 0.2401、最悪の `Xhf0wKl-aBY` が 0.3068。**逆になっている。**
n=5 なので「無効」とまでは言えないが、**選定の根拠に使うのは止めたほうがいい。**

### 8. 検索流入は実質ノイズ

`ひろゆき` 30 / `フランス` 17 / `ひろゆき 切り抜き` 12。あとは「ヤニねこ」「阪神タイガース」
「怪獣の花唄 ギター」など無関係語。**検索を狙う設計には意味が無い。**

## 未検証のもの

- **テーマ差は判定できていない。** n=5 で、しかも全本が同じ配信量を配られている。
  比べるなら同条件で揃えて出す必要がある
- 上の「10%→20% の落ち幅の差は、フックが**完結した主張**か**状況説明**かで決まる」は**仮説**。
  最良3本は「フランス人は歩きスマホをしない」「資格は取れるが実務で通用するか不安」
  「残業は増えるが年収が上がる転職」＝主張。最悪は「降格されて新卒並みの待遇にされた」＝状況説明。
  **測っていない**
- **2本/日にすると本数ぶん伸びるか**は未検証。1本1日で燃え尽き、共食いは観測されていないので
  伸びる見込みはあるが、実測していない
- 投稿時刻 07:00 JST に根拠は無いまま
- インプレッション数とサムネの CTR は **Analytics API では取れない**（Studio の画面のみ）。
  「サムネが悪いのか」は API では判定できない
- 08/31 以降のデータはまだ出ていない（2〜3日遅れ）

## 次にやること

### 1. 冒頭 4〜8秒を作り直す（効果が一番大きい場所）

崖は5本すべて 10%→20% にある。ここを 0.68 ではなく 0.97 側に寄せる。
最有力の手は **相談文の読み上げを冒頭から外し、ひろゆきの結論から始める**こと
（相談内容はテロップで出す）。`recipes/shorts/*.json` の `short.start` と `hook` を触る。

### 2. 登録導線を入れる（ボトルネック）

0.06% は低すぎる。長尺のほうが転換が高い可能性があるので、
ショート → 長尺 → 登録 の導線（概要欄・固定コメント・末尾テロップ）を先に試す。

### 3. 2本/日を試す

1本が1日で燃え尽き、同日に複数本あっても共食いは見えていない。上限は 22本/日（実測）なので
API 側の制約は無い。**在庫が 09/23 で尽きるので、増やすなら先に仕込むこと。**

### 4. 09/05 前後にもう一度測る

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/analytics.py --days 14
```

## 触ってはいけないところ

- **「公式」「公認」と書かない**（権利者の立場は黙認。README 参照）
- 黙認の対象は西村博之氏の素材のみ。共演者の権利は対象外
- 収益化が通ったら必ず <get-clip@razil.jp> へ連絡する
- 切り抜き禁止指定: `exnFXUMMLLI` / `q0GyNI3X8cg`
- 尺は 15 分未満（電話番号未確認のため、超えるとアップロードの処理が中止される）
- カスタムサムネイルは 403 で設定できない（`longUploadsStatus=eligible` ＝電話確認が未実行）。
  ショートには関係ない
- **Studio の UI からのサムネイル適用は自動化できない。** API 経由（`set_thumbnails.py`）を使う
- `published.json` の `privacy_status` は投稿時の申告であって実状態ではない。
  判断の前に `ops_youtube.py --status` を通すこと
- ショートの投稿上限は **22本/日**（2026-08-26 実測）。API クォータとは別物。
  超えると 400 `uploadLimitExceeded` で綺麗に止まる
- `dimensions="day,video"` の同時指定は Analytics API が 400 を返す（未対応クエリ。09/02 実測）。
  1本ずつ `filters=video==ID` で引くこと
- `work/shorts/` に `2026-08-27-pm` 〜 `2026-09-08-pm` の**孤児ディレクトリ**が残っている
  （08/20 セッションの残骸。対応するレシピは無い）。`publish_shorts.py` はレシピ基準なので
  実害は無いが、**移動・改名するときは既存の有無を必ず確認すること**
- `work/5xJ3ZzEnyI8/source.mp4`（3,023MB）は誤って全編取得したもの。削除して差し支えない
- **NISA 回 `u11KwZDUe0s` の公開前に確認**: 解説板で元配信の数字を2箇所訂正している
  （iDeCo の「65歳」→原則60歳、税率「20%」→20.315%）。
  「退職金と iDeCo 一時金の調整期間が5年から10年に延びた」の施行時期は**未確認のまま**
