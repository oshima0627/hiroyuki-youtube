# HANDOFF

最終更新: 2026-08-26

## いま何をしているのか

ひろゆき切り抜きチャンネル「ひろゆき解説ch【切り抜き】」(`UCqK3KYqEeeJiAWr4nSryJYQ` / `@hiroyuki_kaisetsu`) の運用。

2026-08-26 に(1)アカウント調査 (2)ショート28日ぶんの仕込み を実施した。

**方針が変わった。** 「サムネイル／企画が悪くて配信されない」ではなく、
**ショートを毎日出し続けられていなかった**ことが問題だと分かった。長尺は捨て気味にして、
ショート毎日1本を28日続ける。**08/27〜09/23 の28本はすべてビルド済み**で、うち6本は予約投稿済み。

## 検証済みの事実（2026-08-26 に API 出力／Studio 画面で確認）

### アカウントに制限は一切かかっていない

`channels.list(mine=true)` の生レスポンスと全動画の `videos.list` で確認:

| 項目 | 値 |
|---|---|
| `status.privacyStatus` | `public` |
| `status.isLinked` | `true` |
| `status.longUploadsStatus` | **`eligible`**（資格あり・電話確認が未実行なだけ。`disallowed` ではない） |
| `status.isChannelMonetizationEnabled` | `false` |
| 全動画の `failureReason` / `rejectionReason` | **無し** |
| 全動画の `regionRestriction` / `contentRating` | **無し** |
| 全動画の `madeForKids` | `false` |

Studio 画面でも: ダッシュボードに警告バナー無し / 機能の利用資格は 標準=有効・中級者向け=利用資格あり・
上級者向け=利用資格あり / 収益化ページにポリシー違反の表示無し / 視聴者設定は「動画ごとに確認」。

### 兄弟チャンネルとの比較（`scripts/audit_siblings.py`）

同一 Google アカウント `orfevre6.27@gmail.com` 配下に4チャンネル。

| チャンネル | 開設 | 登録 | 公開 | 総視聴 |
|---|---|---|---|---|
| 図解でわかる令和の虎 | 2026-08-10 | 4 | 30 | **24,095** |
| ひろゆき解説ch【切り抜き】 | 2026-08-13 | 0 | 12 | 501 |
| コムドット名場面ch【切り抜き】 | 2026-08-13 | 0 | 14 | 37 |
| 日本の最新ニュースまるわかり | 2025-09-24 | 2,880 | 196 | 1,321,830 |

**「図解でわかる令和の虎」のショート視聴（1本あたり）:**

| 08/10 | 〜08/18 | 08/20 | 08/21 | 08/23 | 08/25 |
|---|---|---|---|---|---|
| 9 | 6〜16 | **5,628** | **6,027** | **5,899** | **1,956** |

開設から9日間は 6〜16回で、ひろゆき解説chと同じ状態だった。10日目に跳ね、以降も回り続けている。
登録者は跳ねた後も4人。**長尺は令和の虎でも 0〜6回**で、3チャンネル共通で回っていない。

これで否定されたもの: アカウント制裁説 / 新規チャンネルだから説 / 切り抜きだから説 /
サムネイルが無いから説 / 8-15 の低CTRで罰を受けた説。

### 止まった理由は「やめた」ではなく「上げ切れなかった」

前セッションは14本ビルドして5本しか上げていなかった。残り9本はビルド済みのまま予約日を過ぎていた。

`videos.insert` は1本 1,600 ユニット、1日の枠は 10,000。**1日5〜6本が上限**で、
`publish_shorts.py` は毎日回す前提。それが回されなかった。**「作る」と「上げ切る」の間に歯止めが無い。**

### 今日やったこと（すべて実行ログで確認済み）

- `scripts/plan_shorts.py` に `--slots` を追加（1日1本にできる。既定は現状の am,pm 2本で不変）
- ビルド済み未投稿の9本を 08/27〜09/04 07:00 JST に振り直し
- 9本の窓を `used_shorts.json` に先行登録（`publish_shorts.mark_used` を再利用）
- 新規19本を計画・ビルド（09/05〜09/23）
- **28本を ffprobe で全件検証: 尺一致・1080x1920・meta整合・md5 重複ゼロ・異常0**
- **6本を予約投稿し、API で実状態を確認**:

| videoId | 公開予定(JST) | 状態 | タイトル |
|---|---|---|---|
| `pCbEawE27Ds` | 08/27 07:00 | private→予約 | フランス人は歩きスマホをしない |
| `HCn_fSMbkDM` | 08/28 07:00 | private→予約 | 資格は取れるが実務で通用するか不安 |
| `IihXyEgECno` | 08/29 07:00 | private→予約 | 「まずい」という言葉を使わない |
| `Xhf0wKl-aBY` | 08/30 07:00 | private→予約 | 降格されて新卒並みの待遇にされた |
| `706Seph9B2Q` | 08/31 07:00 | private→予約 | 残業は増えるが年収が上がる転職 |
| `ITWtz0AMOQY` | 09/01 07:00 | private→予約 | 10歳の娘が寝る前まで宿題をやめない |

6本とも `uploadStatus=processed`、`publishAt` 一致を確認済み。

### 途中で踏んだ罠（同じことを繰り返さないために）

`work/shorts/` に **08/20 セッションの残骸ディレクトリ**（`2026-08-27-am` 〜 `2026-09-08-pm`）が
残っていた。日付の振り直しで `shutil.move(src, dst)` を使ったところ、**dst が既存ディレクトリだと
中に入れ子で移動する**ため、9本が「古い成果物が親、正しい成果物が入れ子」という状態になった。
さらに 09/06〜09/08 は古い `video.mp4` が居座ったせいでビルドがスキップされた。

ffprobe で尺を突き合わせて気づいた。**md5 とファイルサイズの重複が決め手。**
修復済み（入れ子を引き上げ、3本を再ビルド、全件再検証で異常0）。

**教訓: ビルド成果物は「あれば正しい」とは限らない。レシピと突き合わせて検証してから上げること。**

## 未検証のもの

- **「毎日続ければ跳ねる」は成功例 n=1 の仮説。** 令和の虎の跳ねが自然な配信テストによるものか、
  外部要因かは、そのチャンネルの Analytics を見ないと分からない。
  現在の `token.json` は ひろゆき解説ch にバインドされているため取得できない
- 投稿時刻 07:00 JST に**根拠は無い**。既存6本の最多再生（20回）は朝枠だが、その20回は検索流入
- 兄弟3チャンネルの `longUploadsStatus` が `longUploadsUnspecified` なのは、`mine=true` でないため
  「不明」というだけ。「未確認」の意味ではない
- 電話番号の確認が今なら通るかは未検証（「上限に達している」は本人の申告で、実際に試した記録は無い）

## 次にやること

### 1. 残り22本を上げ切る（最重要・ここで前回切れた）

**明日以降、毎日これを回す。** 1回6本まで。4回で終わる。

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/publish_shorts.py --limit 6
```

**日次実行の担保が無いままだと前回と同じ場所で切れる。** タスクスケジューラ登録を検討すること
（オーナー判断で今回は保留にした）。上げ残しはこれで確認できる:

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/publish_shorts.py --dry-run
```

### 2. 長尺2本を公開する（09/07 までに）

`u11KwZDUe0s`（NISA・5:37）と `TtJKOPH0PCU`（不動産・7:50）が **private のまま**。
**09/07 と 09/14 のショートは `2026-08-27-fudosan` を親に持ち、概要欄が非公開動画を指す。**
それまでに不動産回を公開すること。

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/ops_youtube.py --status
```

**NISA 回の公開前に確認**: 解説板で元配信の数字を2箇所訂正している。
iDeCo の「65歳」→原則60歳、税率「20%」→20.315%。
**「退職金とiDeCo一時金の調整期間が5年から10年に延びた」の施行時期は未確認のまま。**

### 3. 09/23 以降の在庫を用意する

28本で在庫は尽きる。`fetch_topics.py --save` で GCD の掲載を取り込む
（**素材の全編ダウンロードは不要**。`--subs-only` ＋ `probe_signals.py --no-audio` で足りる）。

### 4. 跳ねたかどうかを測る（09/07 ごろ）

令和の虎は10日目に跳ねた。08/27 起点なら 09/05 前後。それまでは視聴が一桁でも止めないこと。

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/audit_siblings.py
```

## 触ってはいけないところ

- **「公式」「公認」と書かない**（権利者の立場は黙認。README 参照）
- 黙認の対象は西村博之氏の素材のみ。共演者の権利は対象外
- 収益化が通ったら必ず <get-clip@razil.jp> へ連絡する
- 切り抜き禁止指定: `exnFXUMMLLI` / `q0GyNI3X8cg`
- 尺は 15 分未満（電話番号未確認のため、超えるとアップロードの処理が中止される）
- カスタムサムネイルは 403 で設定できない（`longUploadsStatus=eligible` ＝電話確認が未実行）。
  **ショートには関係ないので、毎日投稿の妨げにはならない**
- **Studio の UI からのサムネイル適用は自動化できない。** API 経由（`set_thumbnails.py`）を使うこと
- `published.json` の `privacy_status` は投稿時の申告であって実状態ではない。
  判断する前に `ops_youtube.py --status` を通すこと
- `work/shorts/` に `2026-08-27-pm` 〜 `2026-09-08-pm` の**孤児ディレクトリ**が残っている
  （08/20 セッションの残骸。対応するレシピは無い）。`publish_shorts.py` はレシピ基準で動くので
  実害は無いが、**ディレクトリを移動・改名するときは既存の有無を必ず確認すること**
- `work/5xJ3ZzEnyI8/source.mp4`（3,023MB）は誤って全編取得したもの。削除して差し支えない
