# HANDOFF

最終更新: 2026-08-26

## いま何をしているのか

ひろゆき切り抜きチャンネル「ひろゆき解説ch【切り抜き】」(`UCqK3KYqEeeJiAWr4nSryJYQ` / `@hiroyuki_kaisetsu`) の運用。

**2026-08-26 にアカウント調査を実施し、前提が一つひっくり返った。**
これまで「8/15 のテスト配信で反応が弱く、チャンネル全体への配信が絞られた」という筋で考えていたが、
**同じ Google アカウント配下の兄弟チャンネル「図解でわかる令和の虎」が、同時期・同条件で
ショート 5,000〜6,000回/本に跳ねている。** アカウント制裁でもチャンネル制裁でもない。

差はほぼ一点、**投稿を続けたかどうか**。詳細は下の比較表。

## 検証済みの事実（すべて 2026-08-26 に API 出力／Studio 画面で確認）

### アカウント本体

ログイン中の Google アカウントは `orfevre6.27@gmail.com`（`youtube.com/account` の画面で確認）。
既定チャンネルは「日本の最新ニュースまるわかり」。配下に4チャンネル。

### 制限は一切かかっていない

`channels.list(mine=true)` の生レスポンスと、全14本の `videos.list` を確認した。

| 項目 | 値 |
|---|---|
| `status.privacyStatus` | `public` |
| `status.isLinked` | `true` |
| `status.longUploadsStatus` | **`eligible`**（＝資格あり・未実行。`disallowed` ではない） |
| `status.isChannelMonetizationEnabled` | `false` |
| 全動画の `failureReason` / `rejectionReason` | **無し** |
| 全動画の `regionRestriction` / `contentRating` | **無し** |
| 全動画の `madeForKids` | `false` |
| `brandingSettings.channel.country` | `JP` |

Studio 画面でも確認済み:

- ダッシュボードに警告バナー・違反警告カードは**無い**
- 設定 → チャンネル → 機能の利用資格: 1.標準機能 **有効** / 2.中級者向け **利用資格あり**（未有効）/ 3.上級者向け **利用資格あり**
- 設定 → チャンネル → 詳細設定 → 視聴者: 「動画ごとに視聴者の設定を確認します」（チャンネル単位で子ども向けにはしていない）
- 収益化ページにポリシー違反の表示は**無い**。参加要件（2026/08/20 時点）は
  登録者 0/1,000、有効な総再生時間 28h/4,000、有効なショート視聴 8回/1,000万

**つまり YouTube 側から止められているものは何も無い。**

### 決定的: 兄弟チャンネルとの横並び比較

`channels.list(forHandle=...)` ＋ `playlistItems` ＋ `videos.list` で4チャンネルを同一条件で取得した
（スクリプト: `scratchpad/audit_siblings.py`、下の「次にやること」に再実行コマンドあり）。

| チャンネル | 開設 | 登録 | 公開 | 総視聴 |
|---|---|---|---|---|
| 図解でわかる令和の虎【令和の虎Second 切り抜き】 | 2026-08-10 | 4 | 30 | **24,095** |
| ひろゆき解説ch【切り抜き】 | 2026-08-13 | 0 | 12 | 501 |
| コムドット名場面ch【切り抜き】 | 2026-08-13 | 0 | 14 | 37 |
| 日本の最新ニュースまるわかり | 2025-09-24 | 2,880 | 196 | 1,321,830 |

**「図解でわかる令和の虎」のショート視聴回数（1本あたり）:**

| 公開日 | ショート | 同日の長尺 |
|---|---|---|
| 08/10 | 9 | 1 |
| 08/11 | 8 | 1 |
| 08/12 | 6 | 0 |
| 08/13 | 7 | 1 |
| 08/14 | 11 | 1 |
| 08/15 | 10 | 1 |
| 08/16 | 13 | 3 |
| 08/17 | 15 | 6 |
| 08/18 | 16 | 2 |
| 08/19 | （投稿なし） | — |
| 08/20 | **5,628** | 6 |
| 08/21 | **6,027** | 2 |
| 08/22 | **4,480** | 1 |
| 08/23 | **5,899** | 0 |
| 08/24 | **2,691** | 1 |
| 08/25 | **1,956** | 0 |

**開設から9日間は 6〜16回**で、ひろゆき解説chと全く同じ状態だった。
10日目（8/20）に跳ね、以降も 2,000〜6,000回で回り続けている。
登録者は跳ねた後も **4人**。登録者数は原因ではない。

一方、**長尺は令和の虎でも 0〜6回**。ここは3チャンネル共通で回っていない。

### この比較が否定したもの

| これまでの説 | 否定する根拠 |
|---|---|
| アカウント全体が配信を絞られている | 同一アカウントの令和の虎が同時期に 5,000回/本で回っている |
| チャンネルが新しいから | 令和の虎は3日しか違わない。登録者4人 |
| 切り抜きだから | 令和の虎も切り抜き |
| カスタムサムネイルが無いから | ショートはサムネイルを使わない。令和の虎も電話確認していない |
| 8/15 の低 CTR で罰を受けた | 令和の虎も最初の9日間は 6〜16回。同じ谷を通ってから跳ねた |

### 3チャンネルの投稿継続日数

| チャンネル | ショート投稿期間 | 跳ねたか |
|---|---|---|
| 令和の虎 | 8/10〜8/25（**16日間、ほぼ毎日**） | 8/20（11日目）に跳ねた |
| ひろゆき解説ch | 8/14〜8/21（8日間・6本）→ **停止** | 跳ねる前に停止 |
| コムドット名場面ch | 8/18〜8/22（5日間）→ **停止** | 跳ねる前に停止 |

### Analytics（YouTube Analytics API v2 で取得）

- `impressions` / `impressionsClickThroughRate` は **API では取れない**（`Unknown identifier`。Studio 限定）
- 日次の視聴は **8/22 が最後。8/23〜8/25 は行が返らない＝0**
- 流入元（全期間）: `RELATED_VIDEO` 233 / `SUBSCRIBER` 232 / `YT_SEARCH` 23 / `SHORTS` 3 / その他6
  （`SUBSCRIBER` は Studio の「ブラウジング機能」に相当。登録者0でも出る）
- 検索語句は 税理士試験 4 / 簿記1級 3 / 日商簿記1級 2 のみ
- 国は JP 474 のみ

### アップロード済みだが未公開の2本

`published.json` と API の両方で確認。**HANDOFF の前版は「NISA は未アップロード」と書いていたが誤り。**

| 動画ID | タイトル | 尺 | 状態 |
|---|---|---|---|
| `u11KwZDUe0s` | 【ひろゆき】新NISAが円安を加速させる理由。NISAとiDeCoの決定的な違い | 5:37 | **private** |
| `TtJKOPH0PCU` | 【ひろゆき】不動産価格が下がっても庶民が買えない理由。不景気で不動産営業が儲かるのはなぜ | 7:50 | **private** |

## 未検証のもの

- **「投稿を続けると跳ねる」は仮説であって因果ではない。** 成功例は令和の虎の n=1。
  跳ねが自然な配信テストによるものか、外部流入や別要因かは、そのチャンネルの Analytics を
  見ないと分からない。現在の `token.json` は ひろゆき解説ch にバインドされているため取得できない
- 令和の虎の3チャンネル分の `longUploadsStatus` は `longUploadsUnspecified` と返るが、これは
  **`mine=true` でないため「不明」を意味するだけ**で、「未確認」の意味ではない。同様に
  `isChannelMonetizationEnabled` が `None` なのも取得権限が無いだけ
- 令和の虎とひろゆき解説chのタイトルの作り（セリフ引用＋ハッシュタグ vs 平板な要約）が
  効いているかどうかは未検証。コムドットも `#コムドット` を入れているが伸びていないので、
  ハッシュタグ単独では説明できない
- 電話番号の確認が今なら通るかは**未検証**（前セッションの「上限に達している」は本人の申告であり、
  実際に試した記録は無い）

## 次にやること

### 1. ショートの投稿を再開して、10日以上続ける（最優先）

これが今回の調査から出た唯一の実行可能な結論。ひろゆき解説chは8日で止めた。令和の虎は11日目に跳ねた。
**止めた側が跳ねなかった、というのが観測されている唯一の差。**

`plan_shorts.py` → `build_short.py` → `publish_shorts.py` の経路は既にある。
毎日1本、最低14日は連続で出す。途中の視聴回数が一桁でも止めない。

### 2. private の2本を公開する

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/ops_youtube.py --status
```

内容を確認してから公開する。**公開前に確かめること**: NISA 回の解説板で元配信の数字を2箇所訂正している。
iDeCo の「65歳」→原則60歳、税率「20%」→20.315%。
**「退職金とiDeCo一時金の調整期間が5年から10年に延びた」の施行時期は未確認のまま。**

### 3. 兄弟チャンネルの比較を再実行する

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/audit_siblings.py
```

（現在はスクラッチパッドにある。恒久化するならこのパスへコピーすること）

### 4. 検索語で束ねた回を作る

`scripts/plan_keyword.py`。設計は `docs/superpowers/specs/2026-08-25-one-question-per-video-design.md`。

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/plan_keyword.py
```

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/plan_keyword.py --word 為替 --out recipes/2026-08-27-kawase.json
```

**ただし長尺は令和の虎でも 0〜6回しか回っていない。** 長尺に投資する優先度は下がった。
**「為替」を作る前に確認**: 使うブロックの片方が `_Qc2ZF3MLO0` で、この回は 2026-08-25 時点の
GCD ページに出てこなかった。掲載から外れたのかは未確認。

### 5. カスタムサムネイルは当面あきらめる

`longUploadsStatus=eligible` は「資格はあるが電話確認が未実行」という意味。
`set_thumbnails.py` は 403 のまま。**ただしショートには関係ないので、上の 1. の妨げにはならない。**

## 触ってはいけないところ

- **「公式」「公認」と書かない**（権利者の立場は黙認。README 参照）
- 黙認の対象は西村博之氏の素材のみ。共演者の権利は対象外
- 収益化が通ったら必ず <get-clip@razil.jp> へ連絡する
- 切り抜き禁止指定: `exnFXUMMLLI` / `q0GyNI3X8cg`
- 尺は 15 分未満（電話番号未確認のため、超えるとアップロードの処理が中止される）
- **Studio の UI からのサムネイル適用は自動化できない。** ファイル入力に画像を流し込んでも
  Studio 側が反応しない（3回試して通らなかった）。API 経由（`set_thumbnails.py`）を使うこと
- `work/5xJ3ZzEnyI8/source.mp4`（3,023MB）は誤って全編取得したもの。区間取得で再現できるので
  削除して差し支えない（オーナー判断待ち）
- `published.json` の `privacy_status` は投稿時の申告であって実状態ではない。
  判断する前に `ops_youtube.py --status` を通すこと
