# HANDOFF

最終更新: 2026-09-02

---

## ▶ このセッションで最初に実行するコマンド

**予約は 10/16 まで埋まっている。急ぐ作業は無い。** まず測る。

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/analytics.py --days 14
```

### 期限のあるもの

**09/07 までに不動産回 `TtJKOPH0PCU` を公開する。** 09/07 と 09/14 のショート
（`bZV29-wLKE8` / `xRNih8qkIdE`）がこれを親に持ち、概要欄が非公開動画を指している。

NISA 回 `u11KwZDUe0s` も private のまま。**公開前に確認**: 解説板で元配信の数字を2箇所
訂正している（iDeCo の「65歳」→原則60歳、税率「20%」→20.315%）。
「退職金と iDeCo 一時金の調整期間が5年から10年に延びた」の施行時期は**未確認のまま**。

---

## いま何をしているのか

ひろゆき切り抜きチャンネル「ひろゆき解説ch【切り抜き】」(`UCqK3KYqEeeJiAWr4nSryJYQ` /
`@hiroyuki_kaisetsu`) の運用。

**跳ねた。** 08/26（PT）を境に日次 0〜15回 → 1,456〜2,338回。「毎日出し続けられていなかった
のが問題」という読みは当たっていた。**配信量の問題は解けた。次のボトルネックは
登録者への転換（0.06%）と、冒頭 4〜8秒の視聴維持率。**

## 今回やったこと（2026-09-02）

### (1) アナリティクスを取る経路を作った

| 変更 | 内容 |
|---|---|
| `scripts/analytics.py` | **新規。** YouTube Analytics API v2。日次・動画別・動画×日・流入元・検索語・年齢性別・端末・国・視聴維持率 |
| `scripts/upload_youtube.py` | `get_service()` から `get_credentials()` を切り出した（Analytics は別サービス）。`_cred_dir()` で git worktree から実行しても本体の `token.json` を見に行く |

`yt-analytics.readonly` スコープは前から取ってあったが一度も使われていなかった。

### (2) 予約を 10/16 まで埋めた（全て API で実状態を確認済み）

**今日 30本を投稿した。09/02 から 10/16 まで、ショートが1日1本で切れ目なく入っている。**

| やったこと | 結果 |
|---|---|
| 待ち6本のショートを投稿 | 09/18〜09/23 |
| 長尺 `2026-08-21-okane2` を投稿 | `uoU2nsKnMrA` / 13分50秒 / **09/12 12:00 JST に予約** |
| ショート7本を新規に投稿 | 09/24〜09/30（`2026-08-21-okane2` 由来） |
| ショート16本を新規に投稿 | 10/01〜10/16（**手書き hook で解放したぶん**、下記(3)） |

### (3) 「フックが1行に収まらない」で捨てられていた16本を解放した

`plan_shorts.py` はクリップの見出しをそのままフックにする。幅968pxに収まらないものは
**捨てる**（末尾を機械で削ると動詞の途中で切れるため。2026-08-19 の実測）。
09/02 時点で、**素材も親も揃っているのに見出しの幅だけで落ちているクリップが16件**あった。

機械には削らせないまま使う経路として、**`clips[].hook`（人が書いたフック）**を足した。

| 変更 | 内容 |
|---|---|
| `scripts/plan_shorts.py` | `make_hook()` が `clips[].hook` を優先するようにした。無ければ従来どおり `title`。手書き hook が収まらないときは別の除外理由として数える |
| `recipes/2026-08-14-shigoto.json` ほか5件 | 16クリップに `hook` を手で書いた（差分は `hook` 行とその手前のカンマだけ） |
| `scripts/verify_shorts.py` | **新規。** 尺・解像度・md5重複・meta整合を検証する。**投稿の前に必ず通す** |

書いたフック（すべて PIL で実測し、56〜76px で1行に収まることを確認済み）:

| 元の見出し | 手書き hook |
|---|---|
| 営業目標120%を「できない」と答えて詰められた | 無理な営業目標を断ったら詰められた |
| インフラエンジニアの派遣から正社員になりたい | 派遣から正社員になりたい |
| 大学生で起業した。就職・学業・企業のどれを捨てるか | 起業した大学生は何を捨てるか |
| 労働でしか自分の存在価値を証明できない | 働くことでしか自分を証明できない |
| （ほか12件。`git show` で全件見られる） | |

### (4) 長尺 `2026-08-15-ningen` は着手して見送った

HANDOFF の「次にやること (a)」だったが、**中を読んで見送りにした。**

- `plan_episode.py` の生出力のままで、`id` も `title` も解説（note）も無い。clips の mp4 は**7件すべて未取得**
- clips[0]（193秒）は**4つの別々の相談が1つに繋がっている**
  （デートの会話 / BeReal / 毒親との裁判 / 休職中の過ごし方）。見出しも4つを `/` で繋いだもので、章タイトルに使えない
- clips[4]「オープンキッチンと仕込みの信頼性」（133秒）は**飲食店の話**で、人間関係のテーマから外れている
- clips[6]「各国への印象と嫌いな国という概念の不在」は**親中か否か・台湾は国か地域か**という politically sensitive な話題。ASR の崩れも激しい（「新中新兵新営新日」）

使えるのは実質4〜5クリップ（約5分40秒）で、**長尺1本のビルドと投稿に見合わない。**
先に(3)をやったほうが本数が多く、素材の追加取得もいらなかった。

## 検証済みの事実

### A. 予約の実状態（2026-09-02 に `ops_youtube.py --status` で全件照合）

**09/02 から 10/16 まで毎日1本、切れ目なし。** 長尺 `uoU2nsKnMrA` が 09/12。
`u11KwZDUe0s`（NISA）と `TtJKOPH0PCU`（不動産）は private のまま。

10/01〜10/16 の16本（`verify_shorts.py` で尺・解像度・md5・meta を照合、**異常0**）:

| 公開(JST) | videoId | 尺 | フック |
|---|---|---|---|
| 10/01 | `AFw0cJlMmzY` | 64s | コミ障だが大学事務職を目指す |
| 10/02 | `FxQT7vKcSZM` | 42s | 派遣から正社員になりたい |
| 10/03 | `yVDGXvz8DdQ` | 44s | 快適に眠れる温度をデータで探す |
| 10/04 | `4yu_i7RH1nE` | 62s | 働くことでしか自分を証明できない |
| 10/05 | `AVwLVMi_4_c` | 40s | フランスに住む理由が自分に無い |
| 10/06 | `rM4ftL719w4` | 58s | 月三十万の積立でFIREできるか |
| 10/07 | `V8z1YsBJe3M` | 44s | 起業した大学生は何を捨てるか |
| 10/08 | `3tjHZxmugwE` | 55s | 在宅テスターから抜け出したい |
| 10/09 | `7nfTKl2Bbow` | 62s | 未経験からフルリモートで働きたい |
| 10/10 | `T9g4Zm8oVeQ` | 65s | フランスでパルクールを見るか |
| 10/11 | `2M-KzHnID_o` | 56s | 6・3・3・4の学制を変えるなら |
| 10/12 | `Eiwh5C540IA` | 39s | 無理な営業目標を断ったら詰められた |
| 10/13 | `RMBxw4tSNBY` | 42s | 年上のバイトにどう接するか |
| 10/14 | `4CIkzIwtq5M` | 52s | ゴマすりで上げた給料は実力か |
| 10/15 | `u_vN9xYpzyI` | 42s | すぐ辞めたくなる性格は直すべきか |
| 10/16 | `5UDR-8tVjAE` | 41s | 復職に引っ越し。大手を辞めるか |

09/24〜09/30 の7本（すべて `2026-08-21-okane2` 由来）:
`zM9PSQYdD50`(41s 年収千四百万だが税金で何も残らない) / `xR-FlQjz0uQ`(44s Switch2の値上げ) /
`osmOViqw6i0`(40s 住宅ローンは固定か変動か) / `UQarAD6osSs`(39s 今から米国株を始めていいのか) /
`Wye2vEUDOz0`(67s 二十五歳、二百万の車) / `V0rWOURVbo8`(40s 資産四千六百万でサイドFIRE) /
`fs9CywDsa4I`(38s NISA以外におすすめの投資はあるか)

### B. 投稿本数の上限は 22本/日ではなかった（実測で訂正）

08/26 に「22本が通り23本目で `uploadLimitExceeded`」と実測して 22本/日と書いていたが、
**09/02（PT）は同じ日に 30本が通った。** 22 は固定値ではない。
`publish_shorts.py` の `DEFAULT_LIMIT` は 15 なので、16本以上は2回に分けて実行すること。

### C. アナリティクス（2026-09-02 に `analytics.py` の出力で確認）

期間 2026-08-13〜2026-09-02。**データは 08/30 までしか無い**（2〜3日遅れる）。
日付は PT なので JST 07:00 公開は前日の行に乗る。

**配信は解けた。SHORTS フィードから来ている。**

```
日次 views:  08/14-08/25 → 0〜265（大半が一桁）
             08/26 1603 / 08/27 1730 / 08/28 1456 / 08/29 2338 / 08/30 1984
流入元:      SHORTS 8259 / SUBSCRIBER 561 / RELATED_VIDEO 446 / YT_SEARCH 174
```

**1本は1日で燃え尽きる。積み上がらない。**

| videoId | 公開日 | +0日 | +1日 | +2日 | +3日 |
|---|---|---|---|---|---|
| `pCbEawE27Ds` | 08/26 | 1599 | 565 | 1 | 5 |
| `HCn_fSMbkDM` | 08/27 | 1158 | 8 | 228 | 0 |
| `IihXyEgECno` | 08/28 | 1446 | 472 | 235 | — |
| `Xhf0wKl-aBY` | 08/29 | 1447 | 191 | — | — |

どの本も 1,200〜2,200 に収まり、**出来の差が到達数に効いていない。**
まだ一定量のテスト配信を配られている段階で、そこから伸びる側には抜けていない。

**登録者に変わっていない ← ボトルネック。**

```
UNSUBSCRIBED 9604 views / SUBSCRIBED 6 views
期間中の subscribersGained: 合計 6
```

内訳: ショート3人（`IihXyEgECno` / `Xhf0wKl-aBY` / `706Seph9B2Q` が各1）、長尺1人
（`f_pERzvz3uA`）、残り2人は動画に紐付かない。**1再生あたりでは長尺のほうが高い**
（長尺 200回で1人 = 0.5%、ショート 約8,600回で3人 = 0.035%）。分子が1と3なので断定はできない。

収益化はショート枠でも長尺枠でも**登録1,000が必須**。総視聴時間はまだ約76時間。

**視聴維持率の崖は5本とも同じ場所。10〜20%（39〜45秒なので 4〜8秒目）。**

| videoId | 見出し | 1% | 10% | 20% | 100% | 平均視聴率 |
|---|---|---|---|---|---|---|
| `pCbEawE27Ds` | フランス人は歩きスマホをしない | 1.47 | 1.33 | **0.97** | 0.48 | **74.9%** |
| `HCn_fSMbkDM` | 資格は取れるが実務で通用するか不安 | 1.45 | 1.38 | 0.87 | 0.44 | 71.7% |
| `706Seph9B2Q` | 残業は増えるが年収が上がる転職 | 1.41 | 1.36 | 0.95 | 0.40 | 67.6% |
| `IihXyEgECno` | 「まずい」という言葉を使わない | 1.40 | 1.27 | 0.89 | 0.40 | 65.1% |
| `Xhf0wKl-aBY` | 降格されて新卒並みの待遇にされた | 1.31 | 1.21 | **0.68** | 0.30 | **54.5%** |

- 1% が 1.0 超え＝ループしている。**入口の掴みは効いている。**
- `relativeRetentionPerformance` は全本 0.44〜0.64。同尺の他動画に対してほぼ中央値。
- **尺は効いていない。**最長45秒が最良、最短39秒が最悪。

**視聴者・その他。**

```
男性 85.5%（25-34 27.3% / 35-44 22.8% / 45-54 17.8%）  国 JP 100%
端末 MOBILE 8124 / DESKTOP 840 / TABLET 390 / TV 239
長尺: 最良 332回、平均視聴率 26〜33%、relativeRetentionPerformance 0.25〜0.5（中央値未満）
検索: ひろゆき30 / フランス17 / ひろゆき 切り抜き12。あとは無関係語（ヤニねこ・阪神タイガース等）
```

### D. `probe_signals` の `score` は維持率を予測していない

最良の `pCbEawE27Ds` が 0.2401、最悪の `Xhf0wKl-aBY` が 0.3068。**逆になっている。**
n=5 なので「無効」とまでは言えないが、**選定の根拠に使うのは止めたほうがいい。**

### E. ショートの在庫はまた尽きた（10/17 以降が空）

```
候補 0本
  除外  56  使用済み
  除外   2  素材ファイルが無い
  除外   1  フックが1行に収まらない（clips[].hook を書けば通る）
  除外   1  レシピに id が無い
```

## 未検証のもの

- **10月の16本は 39〜65秒で、うち8本が50秒超。** これまで実績があるのは 38〜45秒だけ。
  長いほうが伸びるか損かは**未知**。10月前半のデータで見分けが付く
- 09/28 の `Wye2vEUDOz0` は 67秒。同じく未知
- **「10%→20% の落ち幅は、フックが完結した主張か状況説明かで決まる」は仮説。**
  最良3本は主張（「〜しない」「〜か不安」「〜が上がる転職」）、最悪は状況説明（「〜にされた」）。
  **測っていない。** 今回書いた16件の hook は「元の見出しを短くしただけ」で、主張形には寄せていない
- **テーマ差は判定できていない。** n=5 で全本が同じ配信量
- **2本/日にすると本数ぶん伸びるか**は未検証
- 投稿時刻 07:00 JST に根拠は無いまま
- インプレッション数とサムネの CTR は **Analytics API では取れない**（Studio のみ）
- 08/31 以降のアナリティクスはまだ出ていない（2〜3日遅れ）

## 次にやること

### 1. 冒頭 4〜8秒を作り直す（効果が一番大きい場所。在庫より先でよい）

崖は5本すべて 10%→20% にある。**相談文の読み上げを冒頭から外し、結論から始める**
（相談内容はテロップ）。`recipes/shorts/*.json` の `short.start` を後ろにずらし、
`hook` を主張形に書き換える。**`clips[].hook` の経路は今回できたので、あとは書くだけ。**

比べるなら、同じ長尺から出す2本で「主張形」と「状況説明」を1本ずつにして、
`analytics.py --retention <ID>` で 10%→20% の落ち幅を突き合わせる。

### 2. 登録導線を入れる（ボトルネック）

0.06% は低すぎる。長尺のほうが転換が高い可能性があるので、
ショート → 長尺 → 登録の導線（概要欄・固定コメント・末尾テロップ）を先に試す。

### 3. 在庫を作る（10/17 以降が空）

**素材は余っている**（`work/` に subs+signals 付きの配信が31本）。足りないのは
**「長尺レシピ＋その clips の mp4」**で、`plan_shorts.py` はそこからしか候補を取らない。

- **(a) `recipes/_plan-*.json` を1本レシピに起こす**（kaigai 740秒 / kenko 690秒 /
  kigyo 659秒 / kosodate 730秒 / renai 497秒 / shumi 488秒）。
  `id` / `title` / `description` / **各クリップの note（20文字以上・必須）** を書いて:

  ```bash
  cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/fetch_clips.py recipes/<id>.json
  cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/build_episode.py recipes/<id>.json
  cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/upload_youtube.py work/<id> --schedule <UTC>
  ```

  **`build_episode.py` は VOICEVOX の起動が前提**（http://127.0.0.1:50021）。09/02 は起動済みだった。
  **親を投稿してからでないと `plan_shorts.py` に出てこない。**
- **(b) `work/2026-08-22-ai/` はビルド済み（12分16秒）だがレシピが repo に無い。**
  `meta.json` は残っている。レシピを起こし直せば7クリップぶん増える
- **(c) `recipes/2026-08-15-ningen.json` は見送った**（理由は「今回やったこと (4)」）。
  使うなら clips[0] を「デートで会話が続かない」だけに切り詰め（6654〜6788）、
  clips[4] と clips[6] を落とすこと

### 4. 09/05 前後にもう一度測る

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/analytics.py --days 14
```

## 触ってはいけないところ

- **「公式」「公認」と書かない**（権利者の立場は黙認。README 参照）
- 黙認の対象は西村博之氏の素材のみ。共演者の権利は対象外
- 収益化が通ったら必ず <get-clip@razil.jp> へ連絡する
- 切り抜き禁止指定: `exnFXUMMLLI` / `q0GyNI3X8cg`
- **政治的な話題は落とす。** 「親中か否か」「台湾は国か地域か」のような回は、
  `plan_episode.py` の risk 判定を素通りすることがある（`2026-08-15-ningen` clips[6] で確認）
- 尺は 15 分未満（電話番号未確認のため、超えるとアップロードの処理が中止される）
- **カスタムサムネイルは 403。**`uoU2nsKnMrA` でも再現（09/02）。ショートには関係ない
- **Studio の UI からのサムネイル適用は自動化できない。** API 経由（`set_thumbnails.py`）を使う
- **フックを機械で短くしない。** 末尾を削ると動詞の途中で切れる（2026-08-19 実測）。
  短くするなら `clips[].hook` に**人が書く**。書いたら幅を実測すること（文字数では判定しない）
- **投稿の前に `verify_shorts.py` を通す。** 成果物は「あれば正しい」とは限らない
- **未投稿のショートがあるうちに `plan_shorts.py` を回さない。**
  `used_shorts.json` は投稿時にしか印を付けないので、同じ窓が候補として再び出る（09/02 に遭遇）
- `published.json` の `privacy_status` は投稿時の申告であって実状態ではない。
  判断の前に `ops_youtube.py --status` を通すこと
- `publish_shorts.py` の既定は 15本。16本以上あるときは2回実行する
- `dimensions="day,video"` の同時指定は Analytics API が 400 を返す（未対応クエリ。09/02 実測）。
  1本ずつ `filters=video==ID` で引くこと
- `work/shorts/` に `2026-08-27-pm` 〜 `2026-09-08-pm` の**孤児ディレクトリ**が残っている
  （08/20 セッションの残骸。対応するレシピは無い）。`publish_shorts.py` はレシピ基準なので
  実害は無いが、**移動・改名するときは既存の有無を必ず確認すること**
- `work/5xJ3ZzEnyI8/source.mp4`（3,023MB）は誤って全編取得したもの。削除して差し支えない
