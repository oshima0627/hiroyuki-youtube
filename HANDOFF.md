# HANDOFF

最終更新: 2026-09-10（2回目：意見の反映）

---

## ▶ このセッションで最初に実行するコマンド

**A/B の差し替えが「ビルド済み・未アップロード」で止まっている。
下の3つを順番に叩けば完了する。** 09/10 のセッションでは3つとも
権限（auto mode classifier）で弾かれて実行できなかった。

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/ops_youtube.py --unschedule zM9PSQYdD50 osmOViqw6i0 Wye2vEUDOz0 FxQT7vKcSZM 4yu_i7RH1nE rM4ftL719w4
```

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/publish_shorts.py
```

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/ops_youtube.py --publish TtJKOPH0PCU u11KwZDUe0s
```

**順番を守ること。** 1本目で旧6本の予約を外してから 2本目で新6本を上げないと、
**同じ日に2本公開される。**

そのあと **Studio でサムネイルの権限を解く**（`youtube.com/verify` と
設定→チャンネル→機能の利用資格）。これはコードでは直らない。

---

## いま何をしているのか

ひろゆき切り抜きチャンネル「ひろゆき解説ch【切り抜き】」(`UCqK3KYqEeeJiAWr4nSryJYQ` /
`@hiroyuki_kaisetsu`) の運用。

**2026-09-10 に全期間のアナリティクスを取った。読みが変わった。**
配信量の問題は解けているが、**それは天井を1回押し上げただけで積み上がっていない。**
次のボトルネックは (a) ショート冒頭4〜8秒、(b) サムネ403（アカウント側の権限）、
(c) 長尺を止めていること。

## 今回やったこと（2026-09-10）

`python scripts/analytics.py`（全期間 2026-08-13〜2026-09-10）を実行して読んだだけ。
**コードもレシピも投稿も一切触っていない。**

## 検証済みの事実（2026-09-10 の実出力。Analytics は3日遅れでデータは 09/07 まで）

### A. 日次は伸びていない

| 期間 | 日次 views 平均 |
|---|---|
| 08/26–08/31 | 1,792 |
| 09/02–09/07 | 1,526（**−15%**） |

13日間毎日ショートを出して横ばい〜微減。

### B. ショート13本が全部同じ数字に着地している（打ち切られている）

08/26以降の13本の総再生: 1,176 / 1,204 / 1,204 / 1,244 / 1,272 / 1,286 / 1,344 /
1,394 / 1,456 / 1,692 / 1,855 / 2,174 / 2,459。**1本も抜けていない。**
日次グリッドではどれも公開当日に1回バーストし2〜3日で0になる。在庫が効いていない。

- `relativeRetentionPerformance` = **0.44〜0.66**（YouTube全体の同種動画に対し常に中央値以下）
- 崖は上位8本すべて **10%→20%**（40秒尺で **4〜8秒**）。rel の谷も同じ位置
- 終盤の rel は 0.75〜0.92。**終わりは悪くない。冒頭だけが悪い**
- いいね率 **49 / 19,803 = 0.25%**（ショートの健全値は 1〜3%。4〜10倍低い）

### C. 視聴時間の本体は長尺

| | views | 視聴時間 |
|---|---|---|
| ショート(≤180s) | 19,803（90%） | 67.9h（43%） |
| **長尺** | **2,162（10%）** | **91.2h（57%）** |

長尺6本（08/14〜08/20公開）は一度死んだあと **6本同時に 08/29 再点火 → 09/05 まで回り
09/06 に停止**。個別動画ではなくチャンネル単位で推薦が付いて外れている。
流入は `SUBSCRIBER`（ホーム/登録フィード面。登録者9人なので**実体は非登録者へのホーム配信**。
`subscribedStatus` は SUBSCRIBED=13 views）と `RELATED_VIDEO` がほぼ半々。
**RELATED_VIDEO の出どころは全部よそのひろゆき切り抜きチャンネル（10〜23分）。**

**ショート→長尺の受け渡しは実質ゼロ。**

### D. 登録者9人（22,205 views で純増9）

1,000 views あたり **0.45人**。今のペース（1,650 views/日）で 1,000人まで**約3.7年**。
収益化はどちらの経路でも 1,000人必須なので、これが唯一の律速。

### E. 長尺の冒頭のほうがショートより悪い

`jaQZTRkMN4E`（お金7連発 14:05）: 42秒地点で watchRatio 0.58 / **rel 0.35**。
中盤も rel 0.27〜0.39（下位3分の1）。ここは手付かず。

### F. 「15分の壁」はもう存在しない。サムネ403は別の原因

```
channels().list(part=status) → {'longUploadsStatus': 'eligible', 'isLinked': True,
                                'isChannelMonetizationEnabled': False}
```

**15分超はもうアップロードできる。** HANDOFF の前提が古かった。
一方 `thumbnails().set()` を 09/10 に実際に叩くと今も 403:

```
"The authenticated user doesn't have permissions to upload and set custom video thumbnails."
domain: youtube.thumbnail  reason: forbidden  location: Authorization
```

**コードのバグではなくアカウント側の機能利用資格の問題。**Studio で人間が解くしかない。

### G. 検索の非ブランド語の最大クラスタは不動産系

不動産14 / 不動産価格10 / 不動産 下落4 / 住宅ローン4 / 不動産 暴落3 / 不動産 価格2 /
マンション価格2 / マンション価格 下落2 = **計41**。
**それを受ける長尺 `TtJKOPH0PCU` は private のまま。**

### H. 予約の現況（09/07 に `ops_youtube.py --status` で照合済み。今回は触っていない）

- **長尺**: 09/12 `uoU2nsKnMrA` / 09/13 `_FZ9lqEp1kM` / 09/14 `tBuSES75Fj0` /
  09/15 `iZUkqgyyr-Y` / 09/16 `OKDrOKgjoAI` / 09/17 `5AZMp3Ms7Ws` /
  09/18 `i_hBLR8AZEU` / 09/19 `003mlK291Lg`。**毎日12:00 JST、8日連続**
- **ショート**: 09/07〜10/16 が毎日1本。**10/17以降は空**（積み増さないこと。下の 5）
- `TtJKOPH0PCU`（不動産）と `u11KwZDUe0s`（NISA）は private のまま

### H. `plan_shorts.py` の窓の取り方を作り直した（2026-09-10）

**旧実装は字幕キューの境界にスナップしていた。** docstring には「行の途中では
切らない」と書いてあったが、自動字幕のキュー境界は2行ローリング表示の都合で
決まるもので文の切れ目ではない。**書いてある意図と使っている仕組みが違っていた。**

予約済み30本の冒頭を字幕の実文で当たった結果:

- 冒頭が前の話題の文の途中から始まるものが並んでいた
  （「ないですか。」「なと思いますけども。はい。」「これさっき読んだやつじゃん。」）
- **30本中22本**は、ひろゆき氏の答えが始まるまでに**尺の 7〜80%**（中央値およそ35%）
  を相談文の読み上げに使っていた。維持率の崖（10%→20%）はちょうどその最中

直したもの（`scripts/plan_shorts.py`）:

| 追加 | 内容 |
|---|---|
| `sentences()` | キュー内の文字位置を時間に線形で割り当てて**句点で切り直す** |
| `is_question_lead()` | 疑問符で終わる文＝相談文。ASR は語を崩すが**疑問符は崩れにくい** |
| `opens_with_question()` | 窓の冒頭 `OPEN_GUARD` ぶんに相談文が入る窓を捨てる |
| `is_used()` | **使用済みの判定を重なりとフックに変えた**（下記の事故） |

**実素材104クリップでの旧→新（`--write` はしていない）:**

| | 窓が取れた | 文の頭から始まる | 冒頭に相談文 | 答えまでの割合 最大 |
|---|---|---|---|---|
| 旧 | 103 | **14/103** | 28/103 | 188% |
| 新 | **59** | **59/59** | **0/59** | **0%** |

**在庫は 103→59 に減る（−43%）。** これは意図した代償。`OPEN_GUARD` は
0.30〜0.70 を振って決めた（0.30→冒頭に相談文が8本残る／0.60→0本／
0.70 以上は取れる本数が減るだけ）。

**途中で事故を1件見つけて直した。** `used_shorts.json` の照合が窓の位置の
完全一致だったので、窓を動かすと**既出56本が全部「新しい候補」に戻る**。
重なり50%以上またはフック一致で使用済みとみなすように変えた。
`plan_shorts.py --days 7` の実行で 39本が「使用済み」で除外されることを確認済み。

`tests/test_plan_shorts.py` を追加（14 passed）。

### I. NISA回の保留事項は解消した（一次情報で確認）

`recipes/2026-08-26-nisa.json` の解説板にある
「退職金とiDeCo一時金の調整期間が5年から10年に延びた」は**正しい**。

令和7年度税制改正（所得税法等の一部を改正する法律・令和7年法律第13号）で、
**令和8年1月1日以後**に支払を受ける退職手当等について「前年以前**9年内**」に
延長。今日は 2026-09-10 なので既に施行済みで、過去形の記述も正しい。

出典: 国税庁「令和8年分 源泉徴収のあらまし」
<https://www.nta.go.jp/publication/pamph/gensen/aramashi2026/pdf/01.pdf>

**→ NISA回 `u11KwZDUe0s` は公開してよい。**

### J. `ops_youtube.py --publish` を足した

private / 予約 を今すぐ public にする経路が無かったので追加した。
**09/10 のセッションでは権限（auto mode classifier）で弾かれて実行できていない。**
コードは入っているが、**実際に叩いた確認は取れていない。**

### K. ショート冒頭の A/B を組んだ（2026-09-24〜10/07）

**1日おきに差し替える。** 14日間を1日おきに割ると、両群が7曜日をちょうど1回ずつ
含むので曜日の偏りが出ない。

| 群 | 公開日 | 本数 | レシピ |
|---|---|---|---|
| **新**（文境界＋質問ガード） | 09/24, 09/26, 09/28, 10/02, 10/04, 10/06 | **6** | `<日付>-am-v2.json` |
| 旧（対照。触っていない） | 09/25, 09/27, 09/29, 09/30, 10/01, 10/03, 10/05, 10/07 | 8 | `<日付>-am.json` |

**09/30 は新から外した。** 新パイプラインの窓が
「クル園と香水が苦手でニやかなお店なんできません。」＝ 相談者の文（ASR崩れ）で
始まっていたため。**水曜が新群から抜けるので曜日balanceは完全ではない。**

差し替えは**削除ではない。** 旧6本は private のまま残し、予約だけ外す
（`--unschedule`）。消しても取り返せないうえ、残す費用はゼロ。

**ビルドと検証は済んでいる。アップロードだけ残っている。**

```
verify_shorts.py → 6本 / 異常 0本
  2026-09-24-am-v2  63.30s  音楽も映画も、無くても人は死なない
  2026-09-26-am-v2  44.37s  飲食店は落とした食べ物を戻している
  2026-09-28-am-v2  53.70s  日本で石造りの家を建てたい
  2026-10-02-am-v2  63.93s  テムやアリエクはなぜ異常に安いのか
  2026-10-04-am-v2  43.27s  QAエンジニアはAIに持っていかれる
  2026-10-06-am-v2  55.40s  未経験から広報・PR職に就きたい
```

差し替え対象の旧動画: `zM9PSQYdD50` `osmOViqw6i0` `Wye2vEUDOz0`
`FxQT7vKcSZM` `4yu_i7RH1nE` `rM4ftL719w4`

**両群の冒頭の実測（字幕で照合済み）:**

| | 文の頭から始まる | 1文目が薄い | 冒頭に相談文 |
|---|---|---|---|
| 新 | **6/6** | **0/6** | **0/6** |
| 旧 | 0/8 相当 | 3/7 | 4/7 |

**測り方（10/10 以降。API は3日遅れ）:** 両群の `audienceWatchRatio` を
20% 地点で比べる。崖が消えていれば新群のほうが高い。

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/analytics.py --retention <videoId>
```

### L. ヒューリスティックの限界（重要）

**冒頭の判定は3回作り直した。そのたびに別の壊れ方が出た。**

1. 文境界にスナップ → 「はい。」「失礼しましたというわけで」で始まるものが出た
2. 薄い1文目を弾く → 疑問符の無い質問（「〜いいのでしょうか。」）がすり抜けた
3. 疑問の文末も弾く → 前を受ける接続詞（「ただ〜」「で、〜」）で始まるものが出た

**いま残っている穴: 疑問符も疑問の文末も持たない相談文。**
「クル園と香水が苦手で〜できません。」のような、状況を述べるだけの文は
機械では見分けられない（09/30 がこれで、人が見て外した）。

**→ `plan_shorts.py --write` のあと、必ず冒頭の実文を人が読むこと。**
`clips[].hook` を人が書く運用と同じで、ここは最後まで人の目が要る。
正規表現を4つ目に足すより、読んで落とすほうが速い。

## 未検証のもの

- 09/08 以降のアナリティクス（API が3日遅れ。09/11以降にもう一度引けば取れる）
- **`ops_youtube.py --publish` は一度も成功していない**（権限で弾かれた）。
  コードの経路としては通っているが、実際の挙動は未確認
- **新パイプラインの6本は通しで再生していない。** ビルド・尺・解像度・
  冒頭の字幕は確認したが、**映像は見ていない**
- **6本ともまだアップロードしていない**（権限で弾かれた）。A/B は始まっていない
- 在庫は 103→**59** に減った（接続詞ガードまで入れた後の実測）。
  既出29本を引くと**残り30本ぶん**。日々の配信に足りるかは未確認
- `ops_youtube.py --publish` / `--unschedule` はどちらも**一度も実行できていない**
- 09/12〜09/19 の長尺8日連続が効くか
- 冒頭を作り直したら崖が消えるか（**まだ1本も直していない**）
- 09/07 に作った長尺7本は**通しで再生していない**（前回から変わらず）

## 次にやること（優先順。2026-09-10 の実測にもとづく）

### 1. `TtJKOPH0PCU`（不動産 7:50）を公開する

上の G。検索の最大クラスタを受ける唯一の長尺が private で、しかも公開済みショート
`bZV29-wLKE8` と 09/13公開の `xRNih8qkIdE` が概要欄で**死んだリンク**を指している。

`u11KwZDUe0s`（NISA 5:37）も private。公開前の確認事項は据え置き:
解説板で元配信の数値を2箇所訂正済み（iDeCo「65歳」→原則60歳、税率「20%」→20.315%）。
**「退職金とiDeCo一時金の調整期間が5年→10年」の施行時期だけ未確認。**
確認できないならその1文を落として出す。

### 2. サムネの403を Studio で解く（人間の作業。コードでは直らない）

`youtube.com/verify` と 設定→チャンネル→**機能の利用資格**。
長尺のCTRを動かす唯一のレバーが開設以来まるごと使えていない。

### 3. ショート冒頭4〜8秒を作り直す。**ただし半分だけ**

13本で同じ位置に同じ崖があるので仮説は十分に絞れている。
相談文の読み上げを冒頭から外し、答え（結論）から始める。
`recipes/shorts/*.json` の `short.start` を後ろへ、`hook` を主張形に。

**全部を書き換えないこと。半分だけ直して同じ週に出す。**
全部変えると「直したから伸びた」が言えない。
→ そのために **10/16まで埋まっている在庫の一部を差し替える必要がある。**
（在庫を埋めきったこと自体が、いま学習を止めている）

### 4. 長尺の冒頭42秒を作り直す（上の E）

### 5. 10/17以降のショート在庫を積み増すのを止める

効果が測れていないものの生産量を増やしている。**3 の結果を見てから作る。**

### 6. 09/11以降にもう一度測る

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/analytics.py
```

## 触ってはいけないところ

- **「公式」「公認」と書かない**（権利者の立場は黙認。README 参照）
- 黙認の対象は西村博之氏の素材のみ。共演者の権利は対象外
- 収益化が通ったら必ず <get-clip@razil.jp> へ連絡する
- 切り抜き禁止指定: `exnFXUMMLLI` / `q0GyNI3X8cg`
- **政治的な話題は落とす。** `plan_episode.py` の risk 判定を素通りする
  （`2026-08-15-ningen` clips[6]、`_plan-kaigai` clips[6]、`_plan-kosodate` clips[7] で確認）
- **`_plan-*.json` は既存レシピとの重複を見ていない。** 使う前に必ず突き合わせること
  （`plan_episode.py` の `used_ranges` / `overlaps` を呼べば出る）
- ~~尺は15分未満~~ **2026-09-10 に `longUploadsStatus: eligible` を確認。15分の壁はもう無い**
- **カスタムサムネイルは 403。** 09/10 に単発で叩いても再現。**アカウント側の機能利用資格の
  問題**であってコードのバグではない（上の検証済みF）。ショートには関係ない
- **Studio の UI からのサムネイル適用は自動化できない。** API 経由（`set_thumbnails.py`）を使う
- **フックを機械で短くしない。** 末尾を削ると動詞の途中で切れる（2026-08-19 実測）。
  短くするなら `clips[].hook` に**人が書く**。書いたら幅を実測すること
- **投稿の前に `verify_shorts.py` を通す**（ショートの場合）
- **未投稿のショートがあるうちに `plan_shorts.py` を回さない。**
  `used_shorts.json` は投稿時にしか印を付けない
- `published.json` の `privacy_status` は投稿時の申告であって実状態ではない。
  判断の前に `ops_youtube.py --status` を通すこと
- `publish_shorts.py` の既定は 15本。16本以上あるときは2回実行する
- `dimensions="day,video"` の同時指定は Analytics API が 400 を返す。
  1本ずつ `filters=video==ID` で引くこと
- **`_plan-kenko` / `_plan-kosodate` / `_plan-kigyo` はもう使わない。** 上の理由で落としてある
- `recipes/2026-08-15-ningen.json` は 09/02 に見送り済み（理由は git log）
- `work/shorts/` に `2026-08-27-pm` 〜 `2026-09-08-pm` の**孤児ディレクトリ**が残っている。
  実害は無いが、移動・改名するときは既存の有無を必ず確認すること
- `work/2026-08-23-mono/` は途中で止まったビルド（`video.mp4` も `meta.json` も無い）。
  対応するレシピも無い
- `work/5xJ3ZzEnyI8/source.mp4`（3,023MB）は誤って全編取得したもの。削除して差し支えない
