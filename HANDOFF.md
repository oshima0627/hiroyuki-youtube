# HANDOFF

最終更新: 2026-09-07

---

## ▶ このセッションで最初に実行するコマンド

**長尺は 09/19 まで、ショートは 10/16 まで埋まった。急ぐビルドは無い。** まず測る。

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/analytics.py --days 14
```

### 期限を過ぎたまま残っているもの（判断待ち）

**不動産回 `TtJKOPH0PCU` は private のまま。** HANDOFF の前版では「09/07 までに公開する」と
書かれていたが、**09/07 の時点で未対応。** 既に public の `bZV29-wLKE8`（09/07 公開）と
09/13 公開予定の `xRNih8qkIdE` が概要欄でこの非公開動画を指しているので、**リンクが切れている。**
公開するか、ショート側の概要欄を直すかを決めること。

NISA 回 `u11KwZDUe0s` も private のまま。**公開前に確認**: 解説板で元配信の数字を2箇所
訂正している（iDeCo の「65歳」→原則60歳、税率「20%」→20.315%）。
「退職金と iDeCo 一時金の調整期間が5年から10年に延びた」の施行時期は**未確認のまま**。

---

## いま何をしているのか

ひろゆき切り抜きチャンネル「ひろゆき解説ch【切り抜き】」(`UCqK3KYqEeeJiAWr4nSryJYQ` /
`@hiroyuki_kaisetsu`) の運用。

08/26（PT）を境に日次 0〜15回 → 1,456〜2,338回に跳ねた。**配信量の問題は解けている。
次のボトルネックは登録者への転換（0.06%）と、冒頭 4〜8秒の視聴維持率**（前回の実測。今回は測っていない）。

## 今回やったこと（2026-09-07）

### 長尺を一週間ぶん（7本）作って予約した

**09/13〜09/19、毎日 12:00 JST（03:00 UTC）に1本ずつ。全て API で予約を確認済み。**

| 公開(JST) | videoId | 尺 | レシピ | タイトル |
|---|---|---|---|---|
| 09/13 | `_FZ9lqEp1kM` | 12:16 | `2026-08-22-ai` | AIに仕事を取られる話7連発 |
| 09/14 | `tBuSES75Fj0` | 13:02 | `2026-09-07-gaishoku` | 外食の話7連発 |
| 09/15 | `iZUkqgyyr-Y` | 10:04 | `2026-09-07-renai` | 恋愛と結婚の相談6連発 |
| 09/16 | `OKDrOKgjoAI` | 10:05 | `2026-09-07-kaigai` | 海外の相談7連発 |
| 09/17 | `5AZMp3Ms7Ws` | 12:57 | `2026-09-07-mensetsu` | 面接と就職の相談6連発 |
| 09/18 | `i_hBLR8AZEU` | 10:47 | `2026-09-07-shumi` | 趣味と娯楽の話6連発 |
| 09/19 | `003mlK291Lg` | 13:23 | `2026-09-07-business` | ビジネスの話6連発 |

`uoU2nsKnMrA`（お金8連発）が 09/12 なので、**09/12〜09/19 は長尺が8日連続**になる。

### `work/2026-08-22-ai` のレシピを起こし直した（前回の「次にやること 3(b)」）

ビルド済みの `video.mp4` はそのまま投稿した。レシピ `recipes/2026-08-22-ai.json` は
**`meta.json` と `noteimg_NN.txt` と `part_NN.mp4` の実測尺から復元したもの**で、
start/end は次の突き合わせによる**推定**（区間の長さは全て 0.5秒以内で一致した）:

```
part_00 93.3s → VYJ-zTB8TVY  570-664     part_04 63.4s → M7KWrHANxHs 3636-3699
part_01 121.8s → VYJ-zTB8TVY 855-977     part_05 83.7s → jHWUy2IiZvs 1831-1915
part_02 96.8s → VYJ-zTB8TVY 1031-1128    part_06 55.5s → 5xJ3ZzEnyI8 1248-1304
part_03 103.9s → daulqJwmooE 10396-10500
```

note の本文は `noteimg_NN.txt` の中身そのままなので、そこは推定ではない。

### `_plan-kenko` と `_plan-kosodate` は使わなかった

**`_plan-*.json`（08/25 生成）は既存レシピとの重複を除外していない。**

- `_plan-kosodate` は 8件中 5件が `2026-08-16-kosodate.json` と同一区間
- `_plan-kenko` は 8件中 4件が `2026-08-20-kurashi.json` と同一区間

残りだけでは1本にならないので、この2テーマは落とした。代わりに未使用ブロックから
**外食 / 面接 / ビジネス** を組み直した。

### 落としたクリップと理由（`_plan-*` から）

| クリップ | 理由 |
|---|---|
| `_plan-kaigai[1]` 5wwIeF3KgbU 158-458 | 300秒。シャツの話から始まり、ハロウィン・路上飲酒・喫煙・シンガポールと話題が4つ入る |
| `_plan-kaigai[6]` zoh669o7zYc 2853-2994 | **政治的。** ポリコレ・ビーガン・フェミニスト・左翼の話で「三国人は殺せ」という引用が入る |
| `_plan-kosodate[7]` zoh669o7zYc 2674-2853 | 話題が4つ（ケチ／体罰賛成派／ビール／英語のyes-no）。**体罰の是非**を含む |
| `_plan-kenko[2]` M7KWrHANxHs 6655-6790 | 睡眠実験の被験者がなぜ男性かという話。科学の文脈だが**性的な内容として扱われうる** |
| `_plan-kenko[4]` VYJ-zTB8TVY 9518-9565 | 中身はスコープと狙撃の話で、健康テーマから外れている |
| `_plan-kigyo[0]` M7KWrHANxHs 10142-10541 | 399秒。回転寿司の回転率の話から鮮度の話に流れる。切り直す手間に見合わない |

### 区間を切り直したもの（字幕の時刻を見て決めた）

| クリップ | 元 | 直した先 | 理由 |
|---|---|---|---|
| Fydc3YFumow 舌を肥やす | 5903.6-6024.0 | **5903.6-5979.8** | 後半に無関係なワーホリの質問が入る |
| Fydc3YFumow 50歳から英会話 | 4113.2-4202.9 | **4113.2-4193.0** | 末尾に無関係な「離婚」の質問が入る |
| M7KWrHANxHs 面接での嘘 | 2406.4-2507.5 | **2385.8-2507.5** | 元は答えの途中から始まる。質問の前振りから入れた |
| 23vSB2fXjc8 規約違反 | 3456.8-3518.0 | **3456.8-3537.0** | 文が途中で切れる。**ただし 3537 より後は政治家の実名が続くのでそこまで** |
| M7KWrHANxHs コンビニの面接 | 9513.8-9716.2 | **9513.8-9718.5** | 直後に次の相談が始まる位置まで詰めた |
| Fydc3YFumow マッチングアプリ | 7990.1-8071.2 | **7990.1-8068.0** | 「検索できないんですよ」で文が閉じる位置に寄せた |

## 検証済みの事実

### A. 予約の実状態（2026-09-07 に `ops_youtube.py --status` で照合）

- **長尺**: 09/12〜09/19 が毎日1本、切れ目なし（上の表）。全て 15分未満
- **ショート**: 09/07〜10/16 が毎日1本（前回のまま。今回は触っていない）
- `u11KwZDUe0s`（NISA）と `TtJKOPH0PCU`（不動産）は private のまま

### B. ビルドとアップロードは全て通った

- `fetch_clips.py` は 36区間を 403 無しで取得できた（Firefox の cookie / Node の JS ランタイム）
- `build_episode.py` は VOICEVOX 0.25.2 が起動している状態で 6本とも成功
- `recipe.validate()` は7本とも OK
- **カスタムサムネイルは今回も 403。** 7本すべてで再現した（`thumbnails/set` が forbidden）。
  `thumb.png` は冒頭カード（2.5秒）にだけ効いている

### C. `thumbnail.py` の顔スコアによる自動選択は今回も外した

`gaishoku` で候補9点→623秒、候補30点→628秒を選んだが、どちらも**タオルを被って
横を向いているフレーム**だった。`contact_sheet.py` で目視して選び直し、
7本すべて `thumb.at` と `thumb.crop` をレシピに書き込んである（再現可能）。

**crop の上端は 0.11 より下にすること。** 0.07 にしたらテロップ帯の文字が写り込んだ
（`kaigai` で実際に起きた）。下端はスパチャのカードに当たるので、`at` のフレームを見て決める。

## 未検証のもの

- **今回の7本の中身は誰にも見せていない。** ビルドが通ったこと・尺・チャプター位置・
  サムネイルの画は確認したが、**通しで再生していない**
- 長尺が8日連続で並ぶのは初めて。**それが伸びに効くかは未知**
- 前回の課題（冒頭4〜8秒の作り直し・登録導線）は**何も手を付けていない**
- 10月の16本（39〜65秒、うち8本が50秒超）が伸びるかは前回のまま未検証
- `2026-08-22-ai` のレシピの start/end は尺の一致による推定（上記）
- 08/31 以降のアナリティクスは今回取っていない

## 次にやること

### 1. 出した7本のショートを起こす（在庫は 10/17 以降が空のまま）

**親が7本増えたので `plan_shorts.py` の候補が出るようになったはず。**
未投稿のショートは無い（10/16 までは全て投稿済み）ので、回してよい。

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/plan_shorts.py
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/verify_shorts.py
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/publish_shorts.py
```

`gaishoku` の「舌を肥やすと飯がまずくなる」には `clips[].hook` を書いてある。
他は見出しがそのままフックになるので、幅で落ちたぶんは手で `hook` を書き足すこと。

### 2. 冒頭 4〜8秒を作り直す（前回から持ち越し。効果が一番大きい場所）

崖は5本すべて 10%→20% にある。相談文の読み上げを冒頭から外し、結論から始める。
`recipes/shorts/*.json` の `short.start` を後ろにずらし、`hook` を主張形に書き換える。

### 3. 登録導線を入れる（前回から持ち越し。ボトルネック）

0.06% は低すぎる。**長尺が8日連続で並ぶ 09/12〜09/19 は、ショート→長尺の導線を
試すのに向いている。** 概要欄・固定コメント・末尾テロップ。

### 4. 09/14 以降にもう一度測る

```bash
cd C:/Users/oshim/Documents/projects/hiroyuki-youtube; python scripts/analytics.py --days 14
```

## 触ってはいけないところ

- **「公式」「公認」と書かない**（権利者の立場は黙認。README 参照）
- 黙認の対象は西村博之氏の素材のみ。共演者の権利は対象外
- 収益化が通ったら必ず <get-clip@razil.jp> へ連絡する
- 切り抜き禁止指定: `exnFXUMMLLI` / `q0GyNI3X8cg`
- **政治的な話題は落とす。** `plan_episode.py` の risk 判定を素通りする
  （`2026-08-15-ningen` clips[6]、`_plan-kaigai` clips[6]、`_plan-kosodate` clips[7] で確認）
- **`_plan-*.json` は既存レシピとの重複を見ていない。** 使う前に必ず突き合わせること
  （`plan_episode.py` の `used_ranges` / `overlaps` を呼べば出る）
- 尺は 15 分未満（電話番号未確認のため、超えるとアップロードの処理が中止される）
- **カスタムサムネイルは 403。** 09/07 も7本で再現。ショートには関係ない
- **Studio の UI からのサムネイル適用は自動化できない。** API 経由（`set_thumbnails.py`）を使う
- **フックを機械で短くしない。** 末尾を削ると動詞の途中で切れる（2026-08-19 実測）。
  短くするなら `clips[].hook` に**人が書く**。書いたら幅を実測すること
- **投稿の前に `verify_shorts.py` を通す**（ショートの場合）
- **未投稿のショートがあるうちに `plan_shorts.py` を回さない。**
  `used_shorts.json` は投稿時にしか印を付けない
- `published.json` の `privacy_status` は投稿時の申告であって実状態ではない。
  判断の前に `ops_youtube.py --status` を通すこと
- `publish_shorts.py` の既定は 15本。16本以上あるときは2回実行する
- `dimensions="day,video"` の同時指定は Analytics API が 400 を返す。
  1本ずつ `filters=video==ID` で引くこと
- **`_plan-kenko` / `_plan-kosodate` / `_plan-kigyo` はもう使わない。** 上の理由で落としてある
- `recipes/2026-08-15-ningen.json` は 09/02 に見送り済み（理由は git log）
- `work/shorts/` に `2026-08-27-pm` 〜 `2026-09-08-pm` の**孤児ディレクトリ**が残っている。
  実害は無いが、移動・改名するときは既存の有無を必ず確認すること
- `work/2026-08-23-mono/` は途中で止まったビルド（`video.mp4` も `meta.json` も無い）。
  対応するレシピも無い
- `work/5xJ3ZzEnyI8/source.mp4`（3,023MB）は誤って全編取得したもの。削除して差し支えない
